import {
  ContentTypeRequest,
  LookbackWindowRequestBody,
  StatusRequest,
} from '@usenotra/sdk/models/operations';

export const CONTENT_TYPES = Object.values(ContentTypeRequest);
export const LOOKBACK_WINDOWS = Object.values(LookbackWindowRequestBody);
export const POST_STATUSES = Object.values(StatusRequest);
